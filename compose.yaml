services:
  web:
    build:
      context: .
      args:
        VITE_POCKETBASE_URL: ${VITE_POCKETBASE_URL:-https://pocketbase.knowledge.ovh}
    ports:
      - "8080:80"
    restart: unless-stopped
